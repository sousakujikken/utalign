# spikes — アライナ選定時の検証スクリプト (履歴)

製品コードでは使っていません。採用判断の根拠は `docs/feasibility-*.md` を参照してください。

- `spike_hf_ctc.py` Hugging Face の日本語 CTC モデルで強制アライメントを試したスパイク (採用)
- `spike_qwen3fa.py` Qwen3-ForcedAligner を試したスパイク (不採用)

依存パッケージのバージョンは当時のもので、現在の `pyproject.toml` では動かない可能性があります。
